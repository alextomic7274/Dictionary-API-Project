package ie.atu.sw;

import java.util.HashMap;
import java.util.Map;

public class Runner {
	public static void main(String[] args) throws Exception {
		HashMap<String, String> map = new HashMap<>();
		Menu m = new Menu();
		m.startMainMenu();
		/*
		String s = "Six,a.: One more than five; twice three; as six yards.; n.: The number greater by a unit than five; the sum of three and three; six units or objects.; n.: A symbol representing six units as 6 vi. or VI.";
		String[] arr = s.split(",");
		System.out.println(arr[0]);
		System.out.println(arr[1]);
		map.put(arr[0], arr[1]);

		for (Map.Entry<String, String> entry : map.entrySet()) {
		  System.out.println(entry.getKey() + ": " + entry.getValue().toString());
		  }
		  */
	}

}